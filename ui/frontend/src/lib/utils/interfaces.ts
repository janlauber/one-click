export interface ProjectSettings {
    overview: boolean;
    rollouts: boolean;
    image: boolean;
    scale: boolean;
    network: boolean;
    volumes: boolean;
    instances: boolean;
    envs: boolean;
    settings: boolean;
    logs: boolean;
}
